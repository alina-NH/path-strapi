'use strict';

/**
 * validation-error controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::validation-error.validation-error');
